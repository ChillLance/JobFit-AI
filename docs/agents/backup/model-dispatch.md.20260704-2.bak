# 模型調度守則（Model Dispatch）

```yaml
版本: 1.0
日期: 2026-07-04
撰寫者: Claude Fable 5
本檔假設: Agent tool 的 model enum = "sonnet"/"opus"/"haiku"/"fable"；主對話模型為 Sonnet 等級或以上；實測數據見 environment.md §3、§4
失效條件: model enum 變動（新增或移除值）→ 按 maintenance.md 更新本檔選型表後才繼續派工
讀者: 未來 session 的主對話模型（預設 Sonnet）。規則全部可直接執行，不需要再判斷。
```

## 1. 指揮官不下場

主對話（你）只做六件事：理解需求、拆任務、寫派工 prompt、驗收、整合結論、跟使用者對話。
以下情況**必須派 subagent**，不准自己動手（數字是門檻，不是參考）：

| 情況 | 派給 |
|---|---|
| 搜尋／掃描預估要開 ≥10 個檔案 | Explore |
| 只需要單一大檔（>1,000 行）裡的結論，不需要全文 | Explore |
| 同一種修改要套用到 >3 個檔案 | general-purpose |
| 要查 ≥2 個網頁或外部文件來源 | general-purpose（WebSearch/WebFetch 是 deferred tool，subagent 的 prompt 裡註明要先用 ToolSearch 載入） |
| 挖歷史 transcript（*.jsonl，最大 11MB） | Explore（prompt 裡明令禁止整檔讀、只准 Grep） |

**為什麼**：diagnosis.md D7/D8a——主對話 context 被工具回傳灌爆是長 session 失焦的主因；歷史上一次錯誤的 `Read` 灌入 1,020,891 字元。

**例外（自己做，不派工）**：預估 ≤3 次工具呼叫能完成的事。
**為什麼**：實測一次派工最低開銷約 28,000 subagent tokens（見 §7）——比小事本身還貴。

## 2. 派工三件套

每個派工 prompt 必含三段，缺一段就是不合格的派工（模板見 prompt-templates.md）：

1. **目標與動機**——subagent 看不到主對話，不知道為什麼要做這件事。
2. **驗收條件**——可機械檢查的：數字、檔案存在、測試 exit code 0、指定格式。「做好做滿」不是驗收條件。
3. **回報格式**——限定長度與結構（見 §4）。

## 3. 顯式指定 model 與 subagent_type

每次派工都要寫 `model` 參數，不准留空（留空=繼承主對話模型，貴的事便宜做、便宜的事貴著做）。
harness **沒有 effort 參數**——要 subagent 深思，在 prompt 第一行寫「先列出完整計畫，再逐步執行」。

| model | 用在 | 不用在 | 為什麼 |
|---|---|---|---|
| `"haiku"` | 回報內容**你能用一次指令獨立重驗**的任務：read-back 讀檔回報、跑指令抄輸出、格式轉換 | 回報裡的數字或結論**只能信它自己報的**（你無法重算）的任務 | 實測 2026-07-04：haiku 在 47 行檔案裡數 `## ` 開頭行，5 數成 4。格式遵循完美，自產數字不可信——但只要你能重驗，錯了也抓得到 |
| `"sonnet"` | 預設工作馬：搜尋、實作、除錯、重構、寫測試、研究 | 跨 >3 個模組的架構取捨 | 能力/成本平衡點 |
| `"opus"`（或當時 enum 中最強者） | 架構決策、sonnet 連錯 2 次後的升級目標、高風險判斷的第二意見 | 機械工作 | 最貴，只買判斷力 |
| `"fable"` | **不要用**——2026-07-04 之後預期不可用；就算 enum 還列著也不選 | — | 使用者的方案已不含此模型 |

| subagent_type | 用在 | 為什麼 |
|---|---|---|
| `Explore` | 一切唯讀任務（搜尋、read-back、挖 transcript） | 沒有 Write/Edit，物理上不可能手滑改檔 |
| `general-purpose` | 要寫檔、改檔、跑會留痕跡的指令 | 全工具 |
| `Plan` | 只要計畫、不要動手 | 唯讀＋出計畫 |

## 4. 回報合約

subagent 的回報**只准**含：結論、檔案:行號、數字。上限 500 字。
- 長產物（報告、diff、清單、程式碼）一律寫到檔案，回報給路徑＋3 行摘要。
- 超過 20 行的引用禁止直接貼回，落檔傳路徑。
- 找不到就回「未發現」，禁止編造。

**為什麼**：回報全文直接進主對話 context——回報失控等於派工白派（context 照樣爆）。

## 5. 升降級路徑

本節管**出錯後**的升級；**開工前**的預判升級（什麼任務一開始就該用最強模型）見 judgment.md §1。兩者互補：先按 judgment.md §1 預判，預判不需升級的，再按本節的錯誤次數門檻走。

- **haiku 錯 1 次 → 直接改派 sonnet 重做**。不要跟 haiku 來回修。
  為什麼：來回一輪的開銷已超過用 sonnet 重派一次。
- **sonnet 同一子任務連錯 2 次 → 升 opus（或當時最強），且必須附完整失敗軌跡**：做了什麼、實際輸出、與預期差在哪。禁止只把原始任務丟過去。
  為什麼：不附軌跡＝強模型把同樣的死路再走一遍，貴兩倍、結果一樣。
- **強模型解出來之後 → 把解法寫成逐步指令，降回 sonnet/haiku 批次套用**到其餘同型案例。
  為什麼：判斷力只買一次，執行用便宜的。
- **同一目標最多重試 2 輪**。第 2 輪失敗就停：換方法、升級、或按 judgment.md §3 問使用者。
  為什麼：diagnosis.md D8b——歷史上同一錯誤原地重試 10 次，沒有一次成功。重試不是策略。

## 6. 驗證不自驗

做事的 agent 不驗收自己的產出。驗收一律派 fresh-context agent（新派一個，不用 SendMessage 續舊的——舊的帶著同一份誤解）：

| 產出類型 | 驗法 | 判準 |
|---|---|---|
| 檔案 | 派 Explore（haiku 可）read-back：檔案存在？段落齊全？與規格清單逐條比對 | 逐條打勾，缺一即 fail |
| 程式碼 | 跑 `npm test`、`npx tsc --noEmit`、`npm run lint` | 三者 exit code 都是 0 |
| UI | webapp-testing skill 或 preview_screenshot | 截圖與需求描述一致 |
| 高風險判斷（動使用者資料、對外發布、不可逆） | 第二意見：同一問題原樣派給另一個 model，比對兩答案 | 不一致 → 升級或問使用者，不准自己裁決 |

**為什麼**：寫的人和驗的人若是同一個 context，同一個誤解會同時出現在產出與驗證裡——驗了等於沒驗。
**界線澄清**：做事的 agent 自己跑 `npm test` 等指令**不算自驗**——判定者是測試框架的 exit code，不是它自己的眼睛。「自驗」指的是它以自己的閱讀與判斷宣稱產出正確。

## 7. 成本現實（2026-07-04 實測）

- haiku 微任務（1 次工具呼叫）：27,986 subagent tokens / 5.7 秒。
- sonnet 挖掘任務（28 次工具呼叫）：53,367 tokens / 4.6 分鐘。
- 換算：派工的固定開銷約等於主對話 10 次小工具呼叫——這就是 §1 例外條款「≤3 次工具呼叫自己做」的依據。
