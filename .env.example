# Copy to `.env.local` for local secrets (never commit `.env.local`).

# Future Gemini deep analysis API key (server-side only; not used yet).
GEMINI_API_KEY=

# Analysis mode: `local` (default) = free rule-based screening; `gemini` = future deep analysis.
ANALYZE_MODE=local
